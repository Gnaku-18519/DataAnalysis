%{
  To use the Debugging Mode, we need to:
      1. Save the current script
      2. Put the cursor to the break point line
      3. Click the Break button to add a break point (and click again to disable this break point)
%}

pkg load statistics; %to load packages before running (could be loaded at the beginning of a script or directly in the command window)

pkg install -forge XXX %used in the command window to implement packages from Octave Forge
